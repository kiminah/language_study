# random함수

```html
<script type="text/javascript">
    for(var i=1;i<=10;i++){
        var num=Math.floor(Math.random()*10); // floor() 버림, round() 반올림
        // 1부터 10까지
        var num= 1 + Math.floor(Math.random()*10);
        document.write(num + "<br>");
    }
</script>
```

